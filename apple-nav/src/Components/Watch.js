import React, { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';

function Watch(props) {
    // const [watch, setWatch] = useState();
    // useEffect(() => {
    //     setWatch(props.items);
    //     console.log(props.items);
    // }, []);
    return (
        <Link to="/">
        {/* {watch[0]} */}
        watch
        </Link>
    )
}

export default Watch;